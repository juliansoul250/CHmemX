[project]
name = "chmemx"
version = "0.2.0"
description = "Local-first shared AI memory graph with centralized curation and vector routing"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py310"
